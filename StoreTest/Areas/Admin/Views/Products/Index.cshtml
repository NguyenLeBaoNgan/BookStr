
@model IEnumerable<StoreTest.Models.Product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Quản Lí Sách Admin</h2>


<button class="btn btn-default">
    @Html.ActionLink("Create New", "Create")
</button>
  

@*@foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TacGia.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details)
            </td>
            <td>
                <img src="@item.Image" height="150" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsSeller)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OnTop)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeleted)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID })
            </td>
        </tr>
    }*@



<section id="gallery">
    <div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                    <div style="height:500px" class="col-lg-4 mb-4">
                        <div class="card">
                            <img style="width:50%;height:50%" src="@item.Image" alt="" class="card-img-top">
                            <div class="card-body">
                                <h5 class="card-title">@item.Name</h5>
                                @*<p class="card-text">@item.Description</p>*@
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.ID })
                                </td>
                            </div>
                        </div>
                    </div>

            }
                </div>
            </div>
        </section>



